// SQL‚Ì—á(1)
SELECT PRODUCT_ID, COUNT(*) FROM SALES
WHERE PRODUCT_ID LIKE 'A-%'
GROUP BY PRODUCT_ID;

// SQL‚Ì—á(2)
SELECT REPLACE(PRODUCT_ID, '-', ''), COUNT(*) FROM SALES
WHERE PRODUCT_ID LIKE 'A-%'
GROUP BY PRODUCT_ID;
